type Survey {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  isPublished: Boolean!
  title: String!
  description: String!
  endDate: DateTime
  votes: [Vote!]! @relation(name: "SurveyVotes")
}

type Vote {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  survey: Survey! @relation(name: "SurveyVotes")
}